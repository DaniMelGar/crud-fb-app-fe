import { UserRowClickableDirective } from './user-row-clickable.directive';

describe('UserRowClickableDirective', () => {
  it('should create an instance', () => {
    const directive = new UserRowClickableDirective();
    expect(directive).toBeTruthy();
  });
});
